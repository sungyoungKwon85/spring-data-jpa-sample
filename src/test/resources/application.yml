# tip test환경은 완전히 격리되어야 한다. 따라서 메모리 DB를 사용하는 것이 좋다.
# 근데 boot가 기본적으로 memory로 돌린다
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true
#    database-platform: org.hibernate.dialect.H2Dialect
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
`
